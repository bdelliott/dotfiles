# home for virtualenvs, used by virtualenvwrapper
export WORKON_HOME="$HOME/venv"

# setup shell stuffs for virtualenvwrapper
wrapper=`which virtualenvwrapper.sh`
if [ $? -eq 0 ]; then
    . $wrapper
fi
